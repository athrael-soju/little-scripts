# Z-Image-Turbo Configuration

# ==================== Model Settings ====================
# Path to the model. Can be a HuggingFace model ID or local path.
# Default: Tongyi-MAI/Z-Image-Turbo
MODEL_PATH=Tongyi-MAI/Z-Image-Turbo

# ==================== Attention Backend ====================
# Attention backend to use for inference.
# Options: auto, flash, _flash_3, native, xformers, sage, flex
# "auto" will automatically detect and use Flash Attention if available.
ATTENTION_BACKEND=auto

# ==================== Performance Settings ====================
# Enable PyTorch compilation for faster inference (after initial warmup).
# Note: First run will be slower due to compilation overhead.
ENABLE_COMPILE=false

# Enable model warmup at startup.
# This precompiles all resolutions, trading startup time for faster first generations.
ENABLE_WARMUP=false

# ==================== Prompt Enhancement ====================
# DashScope API key for prompt enhancement (optional).
# Get your key from: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=
